import { Request, Response } from 'express';

// Placeholder for send notification endpoint
export default async function handler(req: Request, res: Response) {
  res.status(200).json({ message: 'Notification sent (placeholder)' });
}